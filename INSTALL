To run this application you will need a bunch of gems installed on
your system. The simplest way to get them is:

rake gems:install

To see if everything is working run:

rake cucumber
rake spec

The code is known to work with Rails 2.3.9 and Ruby 1.8.7.